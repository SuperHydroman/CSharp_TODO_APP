<UserControl x:Class="TodoApp.Views.Components.UserNav"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:TodoApp.Views.Components"
             mc:Ignorable="d"
             d:DesignHeight="75" d:DesignWidth="450">
    <Border Background="{StaticResource PanelBackground}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
                
            <Button Grid.Column="0" Style="{StaticResource MenuButton}" Click="ChangeView" Tag="completed">
                <materialDesign:PackIcon Kind="CheckAll" Width="25" Height="25" Foreground="{StaticResource Muted}" />
            </Button>
                
            <!-- Divider -->
            <Border Grid.Column="1" Width="1.5" Height="50" Background="{StaticResource Divider}" VerticalAlignment="Center" />
                
            <Button Grid.Column="2" Style="{StaticResource MenuButton}"  Click="ChangeView" Tag="todos">
                <materialDesign:PackIcon Kind="FormatListCheckbox" Width="25" Height="25" Foreground="{StaticResource Muted}" />
            </Button>
                
            <!-- Divider -->
            <Border Grid.Column="3" Width="1.5" Height="50" Background="{StaticResource Divider}" VerticalAlignment="Center" />
                
            <Button Grid.Column="4" Style="{StaticResource MenuButton}" Click="ChangeView" Tag="deleted">
                <materialDesign:PackIcon Kind="DeleteOutline" Width="25" Height="25" Foreground="{StaticResource Muted}" />
            </Button>
        </Grid>
    </Border>
</UserControl>
